# Contributing

PRs are welcome.

## Style
- Prefer **config-driven** code (use `config/config.yaml`).
- Keep scripts in `scripts_r/` thin; put reusable code in `R/`.
- Write all outputs under `results/` and avoid hard-coded absolute paths.
